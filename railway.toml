[build]
builder = "nixpacks"
buildCommand = "cd backend && npm ci --omit=dev"

[deploy]
startCommand = "cd backend && node migrations/run.js && node src/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

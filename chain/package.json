{
  "name": "volt-casino-chain",
  "private": true,
  "version": "0.1.0",
  "description": "Hardhat test harness for the VOLT casino contracts in ../contracts",
  "scripts": {
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "operator": "hardhat run scripts/operator.js --network localhost",
    "bet": "hardhat run scripts/bet.js --network localhost",
    "cashier": "hardhat run scripts/cashier.js --network localhost",
    "cashier-demo": "hardhat run scripts/cashier-demo.js --network localhost",
    "serve": "node scripts/serve.js",
    "faucet": "node scripts/faucet.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "chai": "^4.5.0",
    "ethers": "^6.16.0",
    "hardhat": "^2.28.6"
  }
}
